# Copy to .signing.env and set your Apple development team and unique app ID.
# Environment overrides take precedence over these local defaults.
DEVELOPMENT_TEAM="${DEVELOPMENT_TEAM:-YOUR_TEAM_ID}"
KEY_RELAY_BUNDLE_ID="${KEY_RELAY_BUNDLE_ID:-com.yourcompany.ipadcontrol}"
